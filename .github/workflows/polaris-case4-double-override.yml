name: Case 4 - exit 8 + mark=success + continue-on-error=true (single job)

on:
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

jobs:
  case-4-only:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Scan (mark=success + continue-on-error=true, expect policy violation)
        id: scan
        continue-on-error: true
        uses: blackduck-inc/black-duck-security-scan@v2
        with:
          polaris_server_url: ${{ vars.POLARIS_URL }}
          polaris_access_token: ${{ secrets.POLARIS_ACCESS_TOKEN }}
          polaris_application_name: 'break-build-test'
          polaris_project_name: 'break-build-test'
          polaris_assessment_types: 'SAST'
          mark_build_status: 'success'
          github_token: ${{ secrets.GITHUB_TOKEN }}
      - run: echo "outcome=${{ steps.scan.outcome }} conclusion=${{ steps.scan.conclusion }}"
